# Pixel art com matrizes

Projeto acadêmico que transforma códigos de uma matriz em uma imagem colorida usando NumPy e Matplotlib.

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python main.py
```

